# Database - Local Development (SQLite)
# Uncomment this for local development:
# DATABASE_URL="file:./dev.db"

# Database - Vercel Production (PostgreSQL)
# These will be automatically set by Vercel when you create a Postgres database:
# POSTGRES_PRISMA_URL="your-vercel-postgres-url"
# POSTGRES_URL_NON_POOLING="your-vercel-postgres-direct-url"

# Vercel Blob Storage (for images)
# This will be automatically set by Vercel when you create a Blob store:
# BLOB_READ_WRITE_TOKEN="your-blob-token"

# For local development with Vercel services, pull with: vercel env pull .env.local

# NextAuth Configuration
NEXTAUTH_URL="http://localhost:3000"  # Change to your Vercel URL in production
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# Admin Credentials (REQUIRED for database seeding)
# The seed script will fail if these are not set
ADMIN_EMAIL="your-admin-email@example.com"
ADMIN_PASSWORD="YourSecurePassword123!"

# Production Notes:
# 1. Generate NEXTAUTH_SECRET with: openssl rand -base64 32
# 2. Use a strong password for ADMIN_PASSWORD (min 8 chars, mix of letters/numbers/symbols)
# 3. Use a real email address for ADMIN_EMAIL
# 4. Never commit the actual .env file to version control
# 5. For Vercel: Set all environment variables in Project Settings → Environment Variables
